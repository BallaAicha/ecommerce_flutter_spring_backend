spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://172.20.10.6:9098/realms/ecommerce
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service
